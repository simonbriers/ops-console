# ops-console currently needs no secrets of its own in .env — every
# per-client secret (admin token, SSH target) lives in clients.json
# instead (see clients.example.json), since that's per-monitored-instance
# data, not app-level config. This file is a placeholder for whenever that
# changes (e.g. an auth layer in front of the dashboard itself).
