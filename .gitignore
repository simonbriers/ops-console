# Secrets — clients.json holds real admin tokens/SSH targets, vault.json
# holds real credentials, onboardings/ holds per-client records incl.
# generated admin passwords. Never commit any of them.
clients.json
vault.json
onboardings/
.env
.env.*
!.env.example

# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/
*.egg-info/

# OS / editor
.DS_Store
.idea/
.vscode/

deploy_log.jsonl
history.jsonl
